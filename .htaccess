<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Redirigir la raíz y cualquier ruta a la carpeta public/ de forma transparente
    RewriteRule ^$ public/ [L]
    RewriteRule (.*) public/$1 [L]
</IfModule>
